name: PHP Lint

on:
  push:
    branches:
      - develop

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installer PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Lint tous les fichiers PHP
        run: |
          for file in $(find . -type f -name "*.php"); do
            echo "🔎 Vérification de $file"
            php -l "$file"
          done
